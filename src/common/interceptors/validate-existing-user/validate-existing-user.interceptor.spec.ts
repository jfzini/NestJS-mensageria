import { ValidateExistingUserInterceptor } from './validate-existing-user.interceptor';

describe('ValidateExistingUserInterceptor', () => {
  it('should be defined', () => {
    expect(new ValidateExistingUserInterceptor()).toBeDefined();
  });
});
